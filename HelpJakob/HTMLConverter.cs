namespace HelpJakob
{
    public static class HTMLConverter
    {
        /// <summary>
        /// Used to convert from plain text to HTML
        /// </summary>
        public static string BodyToHTML(string plainText)
        {
            return "" + plainText + "";
        }
    }
}
